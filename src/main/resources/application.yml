server:
  port: 8080

spring:
  data:
    mongodb:
      host: localhost
      port: 27017

      database: db

  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG

